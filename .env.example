# Copy this file to ".env".
# These scripts authenticate to Gmail/Sheets via OAuth (credentials.json + token.json),
# so no key is strictly required here. Add any environment overrides you introduce.
